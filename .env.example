# API Configuration
# Copy to .env.local and uncomment the appropriate section

# === External Access (via Traefik/NPM) ===
# Use when running frontend locally but connecting to deployed auth service
VITE_API_URL=https://hncms-auth.scarif-0.duckdns.org/api
VITE_AUTH_URL=https://hncms-auth.scarif-0.duckdns.org/api/auth

# === Local Development ===
# Use when running both frontend and auth service locally
# VITE_API_URL=http://localhost:3001/api
# VITE_AUTH_URL=http://localhost:3001/api/auth

# App Configuration
VITE_APP_NAME=HNC Application
