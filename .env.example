# API Configuration
# Copy to .env.local and uncomment the appropriate section

# === External Access (via Traefik/NPM) ===
# Use when running frontend locally but connecting to deployed contacts service
VITE_API_URL=https://hncms-contacts.scarif-0.duckdns.org
VITE_API_KEY=dev-api-key-change-in-production

# === Local Development ===
# Use when running both frontend and contacts service locally
# VITE_API_URL=http://localhost:3002
# VITE_API_KEY=dev-api-key-change-in-production

# App Configuration
VITE_APP_NAME=HNC Contacts
