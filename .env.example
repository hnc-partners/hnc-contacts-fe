# API Configuration
# Copy to .env.local and uncomment the appropriate section

# === API Proxy Target ===
# Defaults to deployed URL - override for local backend dev
# VITE_API_TARGET=http://localhost:3002

# === Auth ===
VITE_AUTH_URL=https://hncms-auth.scarif-0.duckdns.org/api/auth

# App Configuration
VITE_APP_NAME=HNC Contacts
